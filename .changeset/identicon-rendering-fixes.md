---
"sigil": patch
---

Fix identicons rendering on transaction hashes in history detail. Remove duplicate identicon in vault account rows. Replace hand-drawn rhombus with actual app icon in title bar; show vault identicon instead of color dot in dashboard header.
