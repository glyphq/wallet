---
"sigil": patch
---

Fix sheet scroll area clipping (padding between content and edges), absolutely center screen header titles so action buttons no longer shift the title, replace FILTER text and refresh character with consistent icon buttons in transaction history
