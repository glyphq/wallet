---
"sigil": patch
---

Block IPv6 loopback in deep link callback validation, update Cargo metadata, reuse reqwest client via OnceLock, sanitize backup filename, hide TitleBar in fullscreen, guard lock-screen on missing vault, replace setTimeout focus with requestAnimationFrame, validate unlock seeds/wallets length
