---
"sigil": patch
---

Surface failed/expired tx alerts in dashboard instead of silently discarding, fix notification permission re-check on app restart, add USD price display from latest-stats API with USD-to-QU input in send screen, show parse error in request screen instead of silent navigation, fix Space font pair (Space Mono for mono, correct Google URL), add Geist font pair
