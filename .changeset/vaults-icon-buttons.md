---
"sigil": patch
---

Replace vaults header text buttons with icon buttons

Import, watch-only, and new vault actions now use `FolderOpen`, `Eye`,
and `Plus` icons instead of text labels for a cleaner header.
